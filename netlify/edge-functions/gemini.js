// 🚀 Netlify Edge Functions 入口（Netlify Edge Functions entry point）
//
// 逻辑全部在 ../shared-adapter.js（加载 ../src/ 共享核心）。
// All logic lives in ../shared-adapter.js (which loads the shared src/ core).
export { default, config } from '../shared-adapter.js';
